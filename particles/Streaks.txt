#
# emitter
#
150 * cos(TWO_PI * IDX / COUNT + random(0.01,0.02) * TWO_PI) * random(1.0,1.1)
150 * sin(TWO_PI * IDX / COUNT + random(0.01,0.02) * TWO_PI) * random(1.0,1.1)
random(1.0,1.5)
random(1.0,1.5)
TWO_PI * IDX / COUNT
-
-
-
-
0.5 + random(0.6,1.2)
-
random(0.5,0.8)
-
-
#
# Modules
#
-
-
RND + TIMER/TTL * 0.4
-
-
1.0 - lerp(0.0,1.0,NORM)
-
-
1.0 - TIMER/TTL
-
-
-
lerp(100,20,TIMER/TTL) * cos(RND)
lerp(100,20,TIMER/TTL) * sin(RND)